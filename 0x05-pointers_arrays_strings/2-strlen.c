#include "main.h"
#include <string.h>

/**
 * _strlen - Getr the lenght of aa string
 * @s: Input aargument, a string
 * Return: VOid
 */

int _strlen(char *s)
{
	int i;

	i = strlen(s);

	return (i);
}
