#include "main.h"
#include <stdio.h>

/**
 * _puts - Prints a string to the standard output and
 * adds a new line
 * @str: Input parameter, must be a string
 *
 * Return: Void
 */

void _puts(char *str)
{
	puts(str);
}
